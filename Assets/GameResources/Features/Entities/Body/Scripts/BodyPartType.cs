namespace Features.Entities
{
    using UnityEngine;
    using Extensions;

    /// <summary>
    /// SO текстового ID типа части тела.
    /// </summary>
    [CreateAssetMenu(fileName = nameof(BodyPartType), menuName = "Features/Extensions/" + nameof(BodyPartType))]
    public class BodyPartType : ID
    {
        // Необходимая логика реализована в родительском классе.
    }
}