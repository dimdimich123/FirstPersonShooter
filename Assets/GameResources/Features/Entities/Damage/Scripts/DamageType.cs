namespace Features.Entities
{
    using Extensions;
    using UnityEngine;

    /// <summary>
    /// SO текстового ID типа урона.
    /// </summary>
    [CreateAssetMenu(fileName = nameof(DamageType), menuName = "Features/Extensions/" + nameof(DamageType))]
    public class DamageType : ID
    {
        // Необходимая логика реализована в родительском классе.
    }
}